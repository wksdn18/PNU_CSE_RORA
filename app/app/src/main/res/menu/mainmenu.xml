<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <!-- ip/port설정 버튼 -->
    <item
        android:id="@+id/acc"
        android:icon="@drawable/ic_car_collision"
        android:title="table"
        app:showAsAction="always"/>

    <item
        android:id="@+id/pir"
        android:icon="@drawable/ic_face_detect"
        android:title="face"
        app:showAsAction="always"/>

    <item
        android:id="@+id/gas"
        android:icon="@drawable/ic_smoke"
        android:title="smoke"
        app:showAsAction="always"/>

    <item
        android:id="@+id/gps"
        android:icon="@drawable/ic_gps"
        android:title="chart"
        app:showAsAction="always"/>

    <item
        android:id="@+id/chart"
        android:icon="@drawable/ic_mainchart"
        android:title="chart"
        app:showAsAction="always"/>


    <item
        android:id="@+id/calendar"
        android:icon="@drawable/ic_caledar"
        android:title="calendar"
        app:showAsAction="always"/>

</menu>